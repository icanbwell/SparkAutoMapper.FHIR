version: '3.5'
services:
  dev:
    user: root
    build:
      dockerfile: spark.Dockerfile
      context: .
    volumes:
      - ./:/SparkAutoMapper.FHIR/
    container_name: sam_fhir_dev
    working_dir: /SparkAutoMapper.FHIR
